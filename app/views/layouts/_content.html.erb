<div class="wrapper">
  <div class="left-side" ng-class="{'collapsed': application.collapsedMode}">
    <ul class="sidebar-menu" ng-repeat="item in application.navs">
      <li ng-class="{active: application.currentUrl == item.url}">
        <a href="#{{item.url}}">
          <i class="fa" ng-class="item.icon"></i>
          <span>{{item.title}}</span>
        </a>
      </li>
    </ul>
  </div>

  <div ng-view class="right-side" ng-class="{'collapsed': application.collapsedMode}">
  </div>
</div>